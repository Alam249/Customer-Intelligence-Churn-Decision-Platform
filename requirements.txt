# Customer Intelligence & Churn Decision Platform
# Pinned to minor versions for reproducibility; Python 3.10+

# ---- Core data stack ----
pandas==2.1.4
numpy==1.24.4
scipy==1.11.4
pyarrow==14.0.2          # parquet I/O for data/interim and data/processed

# ---- Configuration & environment ----
PyYAML==6.0.1
python-dotenv==1.0.1

# ---- Database ----
SQLAlchemy==2.0.25
psycopg2-binary==2.9.9

# ---- Visualisation ----
matplotlib==3.8.2
seaborn==0.13.1

# ---- Machine learning ----
scikit-learn==1.3.2
joblib==1.5.3   # imported directly to persist the fitted feature-engineering transformer
xgboost==2.1.4
# Pinned to 2.x, not the latest 3.x: shap 0.49.1 (latest) cannot parse
# XGBoost >=3.0's base_score serialization format (shap issue #4202/#4288).
# macOS only: XGBoost needs the OpenMP runtime, not installable via pip —
#   brew install libomp
optuna==4.9.0
shap==0.49.1
lifetimes==0.11.3

# ---- Notebooks ----
jupyter==1.0.0
nbconvert==7.16.4
ipykernel==6.29.4

# ---- API (Step 14) ----
fastapi==0.115.6
uvicorn[standard]==0.34.0

# ---- Testing ----
pytest==8.3.4
httpx==0.28.1   # required by fastapi.testclient.TestClient

# NOTE: packages for later steps are added as those steps are implemented,
# so the environment stays minimal and installable at every commit.
# Planned additions: mlflow, streamlit, evidently, ruff, black.
