# Customer Intelligence & Churn Decision Platform
# Pinned to minor versions for reproducibility; Python 3.10+

# ---- Core data stack ----
pandas==2.1.4
numpy==1.26.4   # bumped from 1.24.4: mlflow's dependency resolution requires it;
                # re-verified all 12 API tests and the full pipeline still pass at this version
scipy==1.11.4
pyarrow==14.0.2          # parquet I/O for data/interim and data/processed

# ---- Configuration & environment ----
PyYAML==6.0.1
python-dotenv==1.0.1

# ---- Database ----
SQLAlchemy==2.0.25
psycopg2-binary==2.9.9

# ---- Visualisation ----
matplotlib==3.8.2
seaborn==0.13.1

# ---- Machine learning ----
scikit-learn==1.3.2
joblib==1.5.3   # imported directly to persist the fitted feature-engineering transformer
xgboost==2.1.4
# Pinned to 2.x, not the latest 3.x: shap 0.49.1 (latest) cannot parse
# XGBoost >=3.0's base_score serialization format (shap issue #4202/#4288).
# macOS only: XGBoost needs the OpenMP runtime, not installable via pip —
#   brew install libomp
optuna==4.9.0
shap==0.49.1
lifetimes==0.11.3

# ---- Notebooks ----
jupyter==1.0.0
nbconvert==7.16.4
ipykernel==6.29.4

# ---- API (Step 14) ----
fastapi==0.115.6
uvicorn[standard]==0.34.0

# ---- Testing ----
pytest==8.3.4
httpx==0.28.1   # required by fastapi.testclient.TestClient

# ---- Dashboard (Step 15) ----
streamlit==1.40.2

# ---- Experiment tracking (Step 16) ----
mlflow==3.15.2

# ---- Code quality (Step 18) ----
ruff==0.16.5
black==26.5.1

# NOTE: Step 19 (drift monitoring) needed no new dependency — PSI and the
# KS test are implemented directly on scipy (already pinned above) rather
# than via a monitoring framework like Evidently, whose dependency footprint
# (a full web framework, telemetry, an NLP toolkit) is disproportionate to
# two well-defined statistics over two dataframes. See src/monitoring.py.
#
# NOTE: Step 20 (uplift modeling) also needed no new dependency — S-/T-/X-
# learner and Qini/AUUC are implemented directly on scikit-learn (already
# pinned above: RandomForestClassifier/Regressor, KFold) rather than via
# `causalml`/`econml`/`scikit-uplift`, for the same reason. See src/uplift.py.

# ---- LLM analyst layer (Step 21) ----
# Both official, minimal vendor SDKs — not a framework like LangChain. The
# tool-calling loop is hand-rolled directly on each (src/llm/providers.py),
# the same reasoning as the Step 19/20 NOTEs above. Provider-agnostic: only
# ONE of ANTHROPIC_API_KEY / OPENAI_API_KEY needs to be set at runtime, but
# both SDKs are installed so either works out of the box.
anthropic==1.2.0
openai==3.5.0

# Packages for later steps are added as those steps are implemented, so the
# environment stays minimal and installable at every commit.
