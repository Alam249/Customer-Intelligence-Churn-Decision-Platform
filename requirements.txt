# Customer Intelligence & Churn Decision Platform
# Pinned to minor versions for reproducibility; Python 3.10+

# ---- Core data stack ----
pandas==2.1.4
numpy==1.24.4
scipy==1.11.4
pyarrow==14.0.2          # parquet I/O for data/interim and data/processed

# ---- Configuration & environment ----
PyYAML==6.0.1
python-dotenv==1.0.1

# ---- Database ----
SQLAlchemy==2.0.25
psycopg2-binary==2.9.9

# ---- Visualisation ----
matplotlib==3.8.2
seaborn==0.13.1

# ---- Machine learning ----
scikit-learn==1.3.2

# ---- Notebooks ----
jupyter==1.0.0
nbconvert==7.16.4
ipykernel==6.29.4

# NOTE: packages for later steps are added as those steps are implemented,
# so the environment stays minimal and installable at every commit.
# Planned additions: xgboost, lightgbm, shap, optuna, mlflow,
# lifetimes, fastapi, uvicorn, streamlit, evidently, pytest, ruff, black.
